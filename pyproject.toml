[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastmcp>=2.10.0", "httpx>=0.27.0", "python-dotenv>=1.0.0", "uvicorn[standard]>=0.30.0", "starlette>=0.41.0", "cryptography>=43.0.0" ]
requires-python = ">=3.10"
